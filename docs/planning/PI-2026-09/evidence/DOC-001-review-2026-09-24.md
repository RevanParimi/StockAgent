# DOC-001 — Second fresh-session hard review

Date: **2026-09-24**. Phase: **fresh-session review** under [REVIEW.md](../REVIEW.md).
Review context: **fresh session**. This conversation did not implement or
self-review the 2026-09-23 remediation. The
[first review](DOC-001-review.md) (2026-09-22, `changes_requested`) is kept
unchanged as the dated record. It is part of the reviewed payload, so it is not
edited here.

**Verdict: `accepted`.** All six findings from the first review are fixed or
correctly routed. No critical, high or medium finding remains. Three low
follow-ups are recorded below with owners.

## Reviewed revision and digest verification

| Item | Value |
|---|---|
| Reviewed revision | `c83214541b1b26c14e66cb4b697288c35a5c2830` (HEAD, clean worktree) |
| Payload last changed | `e8df088`. `c832145` changes only `HANDOFF.md`, which is a bookkeeping exclusion. |
| Implementation base | `d105a44a06d430304a724f34ac3dc329a4ca0d87` |
| Review input (manifest SHA-256) | **`63ff474cec51a5810af84dca279a8fcdaa93239cd65230e6f5cd738b01128dd0`**. `sha256sum` gives the same value for the worktree file, `git show HEAD:` and `git show e8df088:`. |
| `kt_manifest.py verify` | Working tree: 24 files, 0 mismatches. `--rev HEAD`: 24 files, 0 mismatches. |
| KT source digest | `5253529c…` (LF-normalized), which equals the PDF footer's `Source 5253529c4e49` |

Drift found: STATE.json still recorded the superseded review input
`d5516ae0…`, while the receipt and HANDOFF recorded `63ff474c…`. The
bookkeeping was corrected in this phase (R1).

## Contract checked

DOC-001 promises a current-code KT whose header names the revision the body
describes. Current code, locally checked results, dated production
observations and PI targets must stay distinguishable. The PDF must match its
source, and the review input must be reconstitutable from committed bytes.

## Independent adversarial checks

The expected values below come from source, git or a deliberately false input,
never from the KT's own wording.

| Check | Input | Result |
|---|---|---|
| F1 guard: tampered manifest | Copy with the KT's `git_blob` zeroed, plus a nonexistent path | `verify --rev HEAD` exits 1, reporting the KT as `changed` and the extra path as `missing` |
| F1 guard: real manifest against the pre-payload revision | `verify --rev d105a44` | Exits 1 and flags exactly the remediation's payload: KT, PDF, guide, PI README, SA-005/006, three scripts changed, `kt_manifest.py` and the first review missing |
| F3 guard: header regressed to `9a805878` | `check_header_revision` on the edited text | **10 errors**: 9 `core/ipo/*` links (including `narrate.py`) and job `ipo_deep_dive`. This is the first review's F3 evidence, reproduced. |
| F3 guard: other false headers | Unknown revision `ffff…`; edition 2026-09-01; missing header | 1 error each, with the correct message |
| F3 guard: current KT | Declared `d105a44` | 0 errors; 64 linked sources; only `tests/unit/test_ipo_bids.py` changed since the revision (informational, by `a2c19c9`) |
| F4 §10 claims | `channels.py`, `outbox.py`, `settings/base.py`, `config.yaml`, `atlas_store.py` | Each claim matches source. `auto` picks Resend only when `RESEND_API_KEY` is set (`channels.py:128-136`). A lookup exception falls back to `DELIVERY_EMAIL_TO` (`:116-125`), which confirms the documented beta-routing gap. `email_enabled: false` has an env override (`base.py:1026`). Outbox retries 3 times at 1/5/30 minutes (`config.yaml:693-694`). `last_error` is set on retry or dead-letter and cleared on success (`outbox.py:181-198`), with an additive migration (`atlas_store.py:239`). The shared-sender limit is documented at `base.py:1084-1087`. |
| §8 size-tier lean | `brief.py:275-330` | An unknown size takes the scalar thresholds, not the largest tier. The tiers are read from `delivery.brief_ipo_size_tiers`. Matches the KT. |
| §5 new paragraph | `daily_review.py:1095-1114`; `settings/base.py:1092-1099`; `config.yaml:662-668` | "Adapts on every eligible review": true. Every non-paper, non-absurd row calls `WeightAdapter.update` and `save_weight_memory`. The 0.12 is the generic-graph `technical` default. SA-039 is labelled **PI target, not current code**, in both the Markdown and the rendered PDF. |
| F2 PDF | pypdf text and a PyMuPDF render of pages 8 and 17 | "24 possible job IDs" appears once and "23 possible job IDs" never. SA-039, "observe-only", `resolve_recipient`, `last_error`, `EMAIL_TRANSPORT`, `brief_ipo_size_tiers`, "SA-001 through SA-039" and "No d105a44 deployment was inspected" are all present. The pages render cleanly, with no clipped table cells or broken labels. |
| Testing guide | `TEAM_TESTING_GUIDE.md` | 12 duties and 60 cases. Line 5 still says all cases are NOT RUN. 10-E describes a future recipient-isolation check, not an implemented gate. |
| §1 story status claim | STATE.json | 39 SA stories (36 planned, 3 stretch), all `todo`, as §1 states |
| Archive | `docs/archive/…original-before-2026-09-15.pdf` | `1fc2402b…`, byte-for-byte unchanged |

## Findings

| # | Severity | Location | Finding | Disposition |
|---|---|---|---|---|
| F1–F4 (first review) | High/Medium | — | Fixed; each independently re-verified above | Closed |
| F5 (first review) | Medium | `tests/unit/test_atlas_outbox.py` | Still fails identically in the isolated harness. The failure is the documented behaviour: `resolve_recipient` returns `""` with no account row and no ambient address. | Routed; recorded on [SA-005](../stories/SA-005.md) and [SA-006](../stories/SA-006.md). Not a documentation defect. |
| F6 (first review) | Low | `STATE.json` | `updated_at` maintained | Closed |
| R1 | Low | `STATE.json` DOC-001 `review_input_sha256` | Recorded `d5516ae0…` after the payload moved to `63ff474c…` | **Corrected in this phase.** Bookkeeping only; the payload is untouched. |
| R2 | Low | `TECHNICAL_DESIGN.md:265-266`, §1 row at `:24` | The new §5 sentence "On 2026-09-23, production logged `technical` at 0.0 … for 5 of 6 observed tickers" is dated production evidence with no sanitized evidence record. It appears only in commit `e8df088`'s message and in the SA-003/SA-039 cards. §1's production-observation row still lists only the September 10, 15 and 21 sources. The sentence is labelled and dated, and it is not load-bearing for any current-code claim. | **Follow-up → [SA-039](../stories/SA-039.md).** Its implementation must record a sanitized pre-activation weight baseline as the evidence for this sentence. It will also rewrite §5 when the switch lands, so §1's row should name the 2026-09-23 read-only inspection then. |
| R3 | Low | `analysis_data/audit_20260910/test_dependencies` (ignored) used by `run_kt_checks.py` | The harness puts **nse 4.0.1** first on `PYTHONPATH`. `a2c19c9` now requires `nse>=2.0.0,<4.0`, and `.stockai` has 2.1.3. None of the 30 harness files is `test_ipo_bids.py`, the only real-dependency check, so today's result stands. The validation environment still no longer matches the declared runtime. | **Follow-up → [SA-033](../stories/SA-033.md)** (locked dependencies). The KT harness should validate against the lock, not an ad-hoc August folder. Also relevant to SA-005. |

Not a finding: the header declares `d105a44`, but HEAD also contains
`a2c19c9`. That commit changed only `requirements.txt` and a test. The KT
documents no dependency pin, so no body statement became false. The guard's
informational list flags exactly that test file, which is the intended
behaviour.

## Commands, environment and results

Windows, Python 3.13.11, the `.stockai` venv, Git Bash. The worktree was clean
before the review. `data/nse/key_registry.json` was `db6bd9c5…` before and
after.

```text
sha256sum docs/planning/PI-2026-09/evidence/DOC-001-manifest.json      -> 63ff474c…
git show {HEAD,e8df088}:…/DOC-001-manifest.json | sha256sum          -> 63ff474c… (both)
python scripts/docs/kt_manifest.py verify <manifest>                  -> 24 files, 0 mismatches
python scripts/docs/kt_manifest.py verify <manifest> --rev HEAD       -> 24 files, 0 mismatches
python scripts/docs/check_kt_docs.py
  -> 0 errors: 13 documents, 248 local links, 39 PI stories, 24 job IDs,
     13 configuration claims, 18 PDF pages, 41,596 characters,
     header d105a44 (64 sources; changed since: tests/unit/test_ipo_bids.py)
python scripts/docs/run_kt_checks.py --dependency-path analysis_data/audit_20260910/test_dependencies
  -> 324 tests, 2 failures, 0 errors, 0 skipped, 52.77 s
     failures (JUnit): test_atlas_outbox::test_send_row_caps_push_and_passes_html_to_email,
                       test_atlas_outbox::test_send_row_returns_transport_reason
                       ("no email on file for user 'u1'"; = F5, routed)
scratchpad adversarial script (not committed): tampered/missing manifest, five header inputs,
  PDF needles, PyMuPDF render of pages 8 and 17
```

**Not exercised:** no full suite and no Linux/Python 3.11 parity. The F5
environment toggle (`DELIVERY_EMAIL_TO` set/unset) was not re-run, because the
JUnit messages are identical to F5's and the code path was traced instead. No
browser run, live provider, delivery transport, authenticated UI or production
inspection. The PDF was rendered for two pages, not the whole document.
Historical bodies of PRODUCT_MAP and RL_DESIGN were not revalidated, as in both
earlier phases.

## Acceptance checklist

| Criterion | Verdict |
|---|---|
| Source-linked explanations distinguish code, local test, production and planned behaviour | **Met.** F3 and F4 are fixed, and §5's SA-039 text is labelled as a target. R2 is a sourcing follow-up, not a mislabel. |
| Concrete walkthroughs for jobs, three loops, IPO, advice vs portfolio, P/L and evaluation | **Met.** The first review verified these, and the new §8/§10 text was verified here. |
| Every PI story maps to functionality and documentation | **Met.** 39 rows, with dependencies and titles machine-checked. |
| Human testing guidance is separate, initially unexecuted, with no implied approval gate | **Met.** 12 duties and 60 cases, all NOT RUN. |
| Links, inventory, PDF extraction/rendering and relevant tests validated in isolation | **Met, with the routed F5 exception.** 322/324 pass. The 2 failures are a test-isolation defect from `590bc9f` and are reproducible. They expose the behaviour §10 now documents, not an error in it. |
| Changed-file digest and limitations recorded in the receipt | **Met.** The manifest is reconstitutable from committed blobs, and the tamper checks fail as they should. |

## Decision and follow-up state

**`accepted`** for revision `c832145` with review input `63ff474c…`. DOC-001
becomes `done`. `production_verification` stays `not_applicable`
(documentation only).

- Follow-ups: R2 → SA-039, R3 → SA-033 (and SA-005), F5 → SA-005/SA-006. Each
  one has a "Routed input" section on its story card. None is a hidden waiver.
- This does not complete SA-031 or its dependencies SA-024, SA-027 and SA-029,
  and it accepts no remediation. All 39 SA stories remain `todo`.
- Next ready story by STATE order: **SA-039** (P0, Sprint 1, no dependencies).
  It is selected, not started. Its implementation runs in a new conversation.
- Nothing in application source, configuration, deployment, jobs, notifications
  or pushes changed. This phase wrote only this receipt, STATE.json, HANDOFF.md,
  a banner in the implementation receipt, and routed notes on SA-033 and SA-039.
  None of those is a manifest payload file.
