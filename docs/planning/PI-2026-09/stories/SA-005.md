# SA-005 — Establish a clean, isolated test and CI baseline

Priority: **P1** · Sprint: **1** · Provisional estimate: **5 points**

Status lives in [STATE.json](../STATE.json), not this card. Dependencies: none. Evidence: F22 in the [September audit](../../../audit/2026-09-10-repository-production-review.md).

## Intended result

The clean tracked-file checkout has a unit test depending on untracked MARUTI predictions. Local dependency gaps also obscure failures; there is no tracked CI workflow.

Seed deterministic temporary runtime fixtures, declare reproducible Python 3.11 test dependencies and add Linux CI. Block real model, market, Railway, SMTP and push transports. Record supported Windows behavior without weakening assertions.

## Starting points

- `tests/unit/intelligence/rl/eval/test_harness.py`
- `requirements.txt`
- `pyproject.toml`
- `pytest.ini`
- `.github/workflows (new)`

Paths are starting points; inspect current callers before editing. Keep this task bounded. If the implementation cannot fit a focused review, record a split with acceptance criteria in STATE.json before taking on another scope.

## Acceptance criteria

- Full suite runs without .env, production volume or untracked market files.
- The MARUTI discovery test proves discovery using a test-created fixture.
- CI runs appropriate unit/integration checks and a changed-code guard against unlogged broad exception swallowing; existing intentional boundaries are explicitly allowlisted.

## Unit and integration evidence

- Start from a fresh checkout and declared dependencies; run the full suite once.
- Demonstrate accidental outbound transport is rejected by the harness.
- Check parquet/NSE extras and file-replace tests on the supported environments.

Use deterministic fixtures and blocked external transports. Record exact commands, environment, results and any unsupported platform in the evidence receipt. Relevant existing tests plus these independent invariants are required; do not merely assert current implementation outputs.

## Hard review

Reject skipped/xfail conversions that hide the discovered defect. Inspect fixture realism and CI secret permissions.

Apply [REVIEW.md](../REVIEW.md) in a fresh conversation after implementation. No unresolved critical/high defect may be signed off. If the reviewer changes behavior, return to implementation and retest; a self-review is not the fresh-session review.

## Rollout and rollback

No production rollout needed. Keep test dependencies separate where practical; SA-028 owns broader packaging/client optimization.

Local implementation completion and production verification are separate. Read-only inspection is authorized; this card does not authorize sending notifications, deploying, changing production variables, enabling policies, or applying destructive migrations.

## Handoff

At implementation completion, write `evidence/SA-005-implementation.md`, capture the changed-file manifest and diff digest, set status to `review_required`, and update HANDOFF.md. At accepted review write `evidence/SA-005-review.md`, mark code completion, and leave any deployment or future-cohort verification pending explicitly. Execute one phase per conversation.

