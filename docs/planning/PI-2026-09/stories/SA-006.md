# SA-006 — Repair delivery transport and expose dead letters

Priority: **P1** · Sprint: **2** · Provisional estimate: **3 points**

Status lives in [STATE.json](../STATE.json), not this card. Dependencies: none. Evidence: F17 in the [September audit](../../../audit/2026-09-10-repository-production-review.md).

## Intended result

The inspected outbox contains 57 dead email rows while 57 push rows are delivered. Repeated SMTP network errors need a transport-level resolution.

Diagnose current connectivity and configured provider read-only. Implement a supported transport/configuration path plus deterministic retry/dead-letter visibility. Prepare exact external configuration changes before requesting any missing authorization.

## Starting points

- `core/delivery/channels.py`
- `core/delivery/outbox.py`
- `tests/unit/test_delivery_channels.py`

Paths are starting points; inspect current callers before editing. Keep this task bounded. If the implementation cannot fit a focused review, record a split with acceptance criteria in STATE.json before taking on another scope.

## Acceptance criteria

- Transport acceptance and user delivery are reported separately.
- Transient errors retry within policy; permanent auth/config errors stop with actionable diagnostics.
- No duplicate notification is created during retry or replay; historical dead letters are retained.

## Unit and integration evidence

- Mock timeout, rejection, retry-after, success and crash-after-acceptance behavior.
- Verify attachments, recipient isolation, redacted logs and disabled-channel behavior.

Use deterministic fixtures and blocked external transports. Record exact commands, environment, results and any unsupported platform in the evidence receipt. Relevant existing tests plus these independent invariants are required; do not merely assert current implementation outputs.

## Hard review

Review provider idempotency limitations honestly; do not assert exactly-once receipt where unsupported.

Apply [REVIEW.md](../REVIEW.md) in a fresh conversation after implementation. No unresolved critical/high defect may be signed off. If the reviewer changes behavior, return to implementation and retest; a self-review is not the fresh-session review.

## Rollout and rollback

Configuration rollback is documented. A real test email or dead-letter resend requires explicit messaging authorization; leave production verification pending until received.

Local implementation completion and production verification are separate. Read-only inspection is authorized; this card does not authorize sending notifications, deploying, changing production variables, enabling policies, or applying destructive migrations.

## Handoff

At implementation completion, write `evidence/SA-006-implementation.md`, capture the changed-file manifest and diff digest, set status to `review_required`, and update HANDOFF.md. At accepted review write `evidence/SA-006-review.md`, mark code completion, and leave any deployment or future-cohort verification pending explicitly. Execute one phase per conversation.

