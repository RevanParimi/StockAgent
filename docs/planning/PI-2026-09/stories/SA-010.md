# SA-010 — Pass sector benchmarks through technical-data calls

Priority: **P2** · Sprint: **2** · Provisional estimate: **2 points**

Status lives in [STATE.json](../STATE.json), not this card. Dependencies: none. Evidence: F20 in the [September audit](../../../audit/2026-09-10-repository-production-review.md).

## Intended result

get_technical_context calls get_peer_correlation with an automobile-index default for every sector.

Pass a configured benchmark end-to-end, label the returned benchmark and carry insufficient-history provenance. Retain a deliberate generic-sector benchmark policy.

## Starting points

- `core/intelligence/algorithms/indicators/fetcher.py`
- `services/data/context/bundle_builder.py`

Paths are starting points; inspect current callers before editing. Keep this task bounded. If the implementation cannot fit a focused review, record a split with acceptance criteria in STATE.json before taking on another scope.

## Acceptance criteria

- BFSI/IT/renewables do not implicitly call the automobile index.
- An unavailable benchmark is missing evidence, not a measured beta=1/correlation=0.
- Auto behavior remains equivalent with sufficient data.

## Unit and integration evidence

- Assert provider-call arguments for every sector and generic routing.
- Test unequal sessions, short history, all-null returns and unavailable benchmark.

Use deterministic fixtures and blocked external transports. Record exact commands, environment, results and any unsupported platform in the evidence receipt. Relevant existing tests plus these independent invariants are required; do not merely assert current implementation outputs.

## Routed input — 2026-09-23 production assurance review

**Measured 2026-09-23: this is live, not historical.** `^CNXAUTO` (`NIFTY_AUTO_TICKER` and the `automobile` benchmark in `settings/base.py`; also `ui_data.py`) returned "possibly delisted; no price data found" **18×** in the 16:30 review. The PI README's "latest inspected failures are older" no longer holds.

## Hard review

Trace API and scheduled paths; changing only prompt labels or every literal to NIFTY 50 fails review.

Apply [REVIEW.md](../REVIEW.md) in a fresh conversation after implementation. No unresolved critical/high defect may be signed off. If the reviewer changes behavior, return to implementation and retest; a self-review is not the fresh-session review.

## Rollout and rollback

Restore prior configured benchmark mapping if necessary without marking synthetic fallback as observed. Production verification uses correlated fetch logs.

Local implementation completion and production verification are separate. Read-only inspection is authorized; this card does not authorize sending notifications, deploying, changing production variables, enabling policies, or applying destructive migrations.

## Handoff

At implementation completion, write `evidence/SA-010-implementation.md`, capture the changed-file manifest and diff digest, set status to `review_required`, and update HANDOFF.md. At accepted review write `evidence/SA-010-review.md`, mark code completion, and leave any deployment or future-cohort verification pending explicitly. Execute one phase per conversation.

