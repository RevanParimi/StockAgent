# SA-012 — Specify and test the learning target before changing it

Priority: **P1** · Sprint: **3** · Provisional estimate: **3 points**

Status lives in [STATE.json](../STATE.json), not this card. Dependencies: [SA-005](SA-005.md). Evidence: F02, F04 in the [September audit](../../../audit/2026-09-10-repository-production-review.md).

## Intended result

Current direction compares actual close with predicted close, conflating forecast residual and realized direction.

Write a versioned contract for issued price basis, horizon, exchange session, realized return, direction/deadband, forecast residual, calibration and abstention. Define per-dimension eligible targets and a migration policy for legacy labels.

## Starting points

- `core/intelligence/rl/workflows/daily_review.py`
- `core/intelligence/rl/agents/feedback_agent.py`
- `docs/RL_DESIGN.md`

Paths are starting points; inspect current callers before editing. Keep this task bounded. If the implementation cannot fit a focused review, record a split with acceptance criteria in STATE.json before taking on another scope.

## Acceptance criteria

- At issue price 100, forecast 108, realized 103: realized direction is UP and the residual is negative; they are separate fields.
- BUY/SELL/HOLD correctness and transaction-cost assumptions are explicit.
- No retrospective metric is relabeled as a valid prospective result.

## Unit and integration evidence

- Table-driven specification tests cover rising/falling/flat markets, split adjustments, missing close, wrong horizon and exact deadband edges.
- Expected results are computed from the written financial contract independently of implementation.

Use deterministic fixtures and blocked external transports. Record exact commands, environment, results and any unsupported platform in the evidence receipt. Relevant existing tests plus these independent invariants are required; do not merely assert current implementation outputs.

## Hard review

Hard review must challenge whether the target answers the user's question and whether its information existed at issue time. Do not start tuning weights in this story.

Apply [REVIEW.md](../REVIEW.md) in a fresh conversation after implementation. No unresolved critical/high defect may be signed off. If the reviewer changes behavior, return to implementation and retest; a self-review is not the fresh-session review.

## Rollout and rollback

Documentation/schema contract only; behavior remains legacy until SA-014's dark implementation is reviewed.

Local implementation completion and production verification are separate. Read-only inspection is authorized; this card does not authorize sending notifications, deploying, changing production variables, enabling policies, or applying destructive migrations.

## Handoff

At implementation completion, write `evidence/SA-012-implementation.md`, capture the changed-file manifest and diff digest, set status to `review_required`, and update HANDOFF.md. At accepted review write `evidence/SA-012-review.md`, mark code completion, and leave any deployment or future-cohort verification pending explicitly. Execute one phase per conversation.

