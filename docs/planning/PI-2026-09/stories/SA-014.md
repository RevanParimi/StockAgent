# SA-014 — Grade frozen decisions and issue controls before their outcomes

Priority: **P1** · Sprint: **3** · Provisional estimate: **5 points**

Status lives in [STATE.json](../STATE.json), not this card. Dependencies: [SA-003](SA-003.md), [SA-013](SA-013.md). Evidence: F02, F03 in the [September audit](../../../audit/2026-09-10-repository-production-review.md).

## Intended result

D-1 grading currently uses fresh post-close verdicts, and the control target can already be closed. Implement contract v2 in a dark lane.

Resolve one matured outcome to the frozen forecast ID. Remove current-day analysis from historical correctness. Schedule a future control target and retain legacy/v2 diagnostics separately.

## Starting points

- `core/intelligence/rl/workflows/daily_review.py`
- `core/intelligence/rl/agents/control_lane.py`
- `core/intelligence/rl/agents/feedback_agent.py`

Paths are starting points; inspect current callers before editing. Keep this task bounded. If the implementation cannot fit a focused review, record a split with acceptance criteria in STATE.json before taking on another scope.

## Acceptance criteria

- Changing today's scores cannot change yesterday's frozen forecast correctness.
- Agent and control use the same admissible information cutoff and target session.
- Missing/late outcomes remain pending/excluded with reasons; dark grading does not mutate production weights.

## Unit and integration evidence

- Integration test replays a post-close job with adversarial changed fresh scores.
- Test future control target, delayed close availability, adjusted price basis and multiple revisions.

Use deterministic fixtures and blocked external transports. Record exact commands, environment, results and any unsupported platform in the evidence receipt. Relevant existing tests plus these independent invariants are required; do not merely assert current implementation outputs.

## Hard review

Reviewer reconstructs at least one row from issuance through outcome without looking at later information. Audit all reward consumers.

Apply [REVIEW.md](../REVIEW.md) in a fresh conversation after implementation. No unresolved critical/high defect may be signed off. If the reviewer changes behavior, return to implementation and retest; a self-review is not the fresh-session review.

## Rollout and rollback

Keep v2 dark until its dependency reviews pass. Activation requires a named rollout and rollback; no retrospective overwrite of legacy outcomes.

Local implementation completion and production verification are separate. Read-only inspection is authorized; this card does not authorize sending notifications, deploying, changing production variables, enabling policies, or applying destructive migrations.

## Handoff

At implementation completion, write `evidence/SA-014-implementation.md`, capture the changed-file manifest and diff digest, set status to `review_required`, and update HANDOFF.md. At accepted review write `evidence/SA-014-review.md`, mark code completion, and leave any deployment or future-cohort verification pending explicitly. Execute one phase per conversation.

