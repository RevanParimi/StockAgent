# SA-017 — Attach historical lineage and quarantine incomparable feedback

Priority: **P1** · Sprint: **4** · Provisional estimate: **3 points**

Status lives in [STATE.json](../STATE.json), not this card. Dependencies: [SA-009](SA-009.md), [SA-013](SA-013.md). Evidence: F12 in the [September audit](../../../audit/2026-09-10-repository-production-review.md).

## Intended result

All 935 inspected feedback rows report automobile sector after legacy defaulting. Evaluation must not assume those labels are verified.

Implement the reviewed manifest reader and canonical history index. Keep original rows, separate inferred/verified/unknown lineage, and reject wrong-roster or conflicting duplicates from comparable cohorts.

## Starting points

- `core/intelligence/rl/stores/prediction_store.py`
- `core/intelligence/rl/eval`

Paths are starting points; inspect current callers before editing. Keep this task bounded. If the implementation cannot fit a focused review, record a split with acceptance criteria in STATE.json before taking on another scope.

## Acceptance criteria

- Every included row has a canonical ID, provenance status and declared grading version.
- Duplicates count once; conflicts and wrong rosters appear in an exclusion ledger.
- No historical claim of causal adaptation is created by metadata repair.

## Unit and integration evidence

- Load mixed legacy/v2 histories across directories and month boundaries.
- Test same ticker under two sectors, identical and conflicting duplicate IDs, and unknown issuance time.

Use deterministic fixtures and blocked external transports. Record exact commands, environment, results and any unsupported platform in the evidence receipt. Relevant existing tests plus these independent invariants are required; do not merely assert current implementation outputs.

## Hard review

Check raw/accepted/excluded accounting conserves rows and cohort exclusions are not selected based on good/bad returns.

Apply [REVIEW.md](../REVIEW.md) in a fresh conversation after implementation. No unresolved critical/high defect may be signed off. If the reviewer changes behavior, return to implementation and retest; a self-review is not the fresh-session review.

## Rollout and rollback

Migration is copy/quarantine with hashes and rollback; production application remains a separate recorded verification step.

Local implementation completion and production verification are separate. Read-only inspection is authorized; this card does not authorize sending notifications, deploying, changing production variables, enabling policies, or applying destructive migrations.

## Handoff

At implementation completion, write `evidence/SA-017-implementation.md`, capture the changed-file manifest and diff digest, set status to `review_required`, and update HANDOFF.md. At accepted review write `evidence/SA-017-review.md`, mark code completion, and leave any deployment or future-cohort verification pending explicitly. Execute one phase per conversation.

