# SA-018 — Build complete, matched monthly evaluation cohorts

Priority: **P1** · Sprint: **4** · Provisional estimate: **5 points**

Status lives in [STATE.json](../STATE.json), not this card. Dependencies: [SA-014](SA-014.md), [SA-017](SA-017.md). Evidence: F12, F13, F14 in the [September audit](../../../audit/2026-09-10-repository-production-review.md).

## Intended result

Month-start reports can run before the final D-1 review, and duplicate stores can inflate aggregates while overwriting per-ticker results.

Build from completed-session watermarks, explicit matched forecast IDs and versioned cutoffs. Publish raw/eligible/excluded counts, report revisions and whole-cohort lift; preserve discordant-only diagnostics under distinct labels.

## Starting points

- `core/intelligence/rl/eval/scorecard.py`
- `services/scheduler/python/scheduler.py`

Paths are starting points; inspect current callers before editing. Keep this task bounded. If the implementation cannot fit a focused review, record a split with acceptance criteria in STATE.json before taking on another scope.

## Acceptance criteria

- Final-session outcomes are included once mature, or the report is explicitly provisional.
- Per-ticker totals reconcile exactly with aggregate IDs.
- Agent/control/base comparisons expose matched denominators and legacy replay limitations.

## Unit and integration evidence

- Test month end on weekend/holiday, delayed final review, late arrival and deterministic rerun.
- Test duplicate stores, unequal agent/control coverage and 15-vs-8 discordants without presenting subset lift as whole-cohort lift.

Use deterministic fixtures and blocked external transports. Record exact commands, environment, results and any unsupported platform in the evidence receipt. Relevant existing tests plus these independent invariants are required; do not merely assert current implementation outputs.

## Hard review

Reviewer independently rebuilds a tiny report from frozen rows and verifies all denominators, cutoffs and revision semantics.

Apply [REVIEW.md](../REVIEW.md) in a fresh conversation after implementation. No unresolved critical/high defect may be signed off. If the reviewer changes behavior, return to implementation and retest; a self-review is not the fresh-session review.

## Rollout and rollback

Keep prior reports immutable and label superseding revisions. Do not claim repaired historical reports prove prospective learning.

Local implementation completion and production verification are separate. Read-only inspection is authorized; this card does not authorize sending notifications, deploying, changing production variables, enabling policies, or applying destructive migrations.

## Handoff

At implementation completion, write `evidence/SA-018-implementation.md`, capture the changed-file manifest and diff digest, set status to `review_required`, and update HANDOFF.md. At accepted review write `evidence/SA-018-review.md`, mark code completion, and leave any deployment or future-cohort verification pending explicitly. Execute one phase per conversation.

