# SA-019 — Evaluate suggestions across weekly and fortnightly cohorts

Priority: **P1** · Sprint: **4** · Provisional estimate: **5 points**

Status lives in [STATE.json](../STATE.json), not this card. Dependencies: [SA-018](SA-018.md). Evidence: F05, F06 in the [September audit](../../../audit/2026-09-10-repository-production-review.md).

## Intended result

Current trend repeats ensemble hit rate for each agent and weekly review can omit exited positions. Publish the comparisons the user actually asked for.

Use the canonical advice/outcome ledger and frozen forecast targets. Define disjoint weekly and 10-trading-session cohorts across month boundaries, comparing like horizons with completed outcomes only. Include exits, abstentions and unavailable prices explicitly.

## Starting points

- `core/delivery/weekly.py`
- `core/intelligence/rl/workflows/daily_review.py`
- `core/portfolio/store.py`

Paths are starting points; inspect current callers before editing. Keep this task bounded. If the implementation cannot fit a focused review, record a split with acceptance criteria in STATE.json before taking on another scope.

## Acceptance criteria

- Sold/exited recommendations remain in the original issued cohort.
- Per-dimension trends use eligible dimension outcomes, not one copied ensemble rate.
- Each period includes count, coverage, exclusions, comparable baseline, uncertainty and cost-aware virtual return where defined.

## Unit and integration evidence

- Fixture spans month boundary, holiday, exited holding, delayed price and overlapping horizons.
- Regression demonstrates two opposite dimensions have different trends when their issued signals differ.
- Verify prior completed fortnight vs current completed fortnight uses identical horizon rules.

Use deterministic fixtures and blocked external transports. Record exact commands, environment, results and any unsupported platform in the evidence receipt. Relevant existing tests plus these independent invariants are required; do not merely assert current implementation outputs.

## Hard review

Trace every recommendation to issuance and maturity; challenge survivorship bias, variable-age marking and favorable-only presentation.

Apply [REVIEW.md](../REVIEW.md) in a fresh conversation after implementation. No unresolved critical/high defect may be signed off. If the reviewer changes behavior, return to implementation and retest; a self-review is not the fresh-session review.

## Rollout and rollback

Reports can ship labeled insufficient data. Keep current deliveries compatible; no manual production send without messaging authorization.

Local implementation completion and production verification are separate. Read-only inspection is authorized; this card does not authorize sending notifications, deploying, changing production variables, enabling policies, or applying destructive migrations.

## Handoff

At implementation completion, write `evidence/SA-019-implementation.md`, capture the changed-file manifest and diff digest, set status to `review_required`, and update HANDOFF.md. At accepted review write `evidence/SA-019-review.md`, mark code completion, and leave any deployment or future-cohort verification pending explicitly. Execute one phase per conversation.

