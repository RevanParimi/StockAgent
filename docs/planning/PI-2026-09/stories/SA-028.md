# SA-028 — Repair packaging and compile the browser client

Priority: **P2** · Sprint: **stretch** · Provisional estimate: **5 points**

Status lives in [STATE.json](../STATE.json), not this card. Dependencies: [SA-001](SA-001.md), [SA-005](SA-005.md). Evidence: F23 in the [September audit](../../../audit/2026-09-10-repository-production-review.md).

## Intended result

Packaging references an unresolved setuptools backend, production dependencies include optional heavy components, and the browser compiles development JSX.

Fix the build backend, split optional/test dependencies and introduce a minimal reproducible production client build. Measure artifact size/build/start behavior before and after. This is a separate follow-on, not part of the XSS hotfix.

## Starting points

- `pyproject.toml`
- `requirements.txt`
- `Dockerfile`
- `src/frontend/prototypes/index.html`

Paths are starting points; inspect current callers before editing. Keep this task bounded. If the implementation cannot fit a focused review, record a split with acceptance criteria in STATE.json before taking on another scope.

## Acceptance criteria

- A clean package/image build uses declared Python 3.11 dependencies.
- Production browser assets do not require development React or runtime Babel.
- Disabled RAG/C++/C# paths have explicit supported install/retirement decisions.

## Unit and integration evidence

- Smoke installed-package imports and production asset routes in an isolated image.
- Browser smoke covers auth, chat streaming, portfolio and PWA cache update; sanitizer regression remains green.

Use deterministic fixtures and blocked external transports. Record exact commands, environment, results and any unsupported platform in the evidence receipt. Relevant existing tests plus these independent invariants are required; do not merely assert current implementation outputs.

## Hard review

Review supply-chain pinning, stale service-worker assets, path compatibility and measured performance claims.

Apply [REVIEW.md](../REVIEW.md) in a fresh conversation after implementation. No unresolved critical/high defect may be signed off. If the reviewer changes behavior, return to implementation and retest; a self-review is not the fresh-session review.

## Rollout and rollback

Rollback to prior verified assets/image with cache-version handling; never roll back the security fix. Stretch work waits until promoted in STATE.json.

Local implementation completion and production verification are separate. Read-only inspection is authorized; this card does not authorize sending notifications, deploying, changing production variables, enabling policies, or applying destructive migrations.

## Handoff

At implementation completion, write `evidence/SA-028-implementation.md`, capture the changed-file manifest and diff digest, set status to `review_required`, and update HANDOFF.md. At accepted review write `evidence/SA-028-review.md`, mark code completion, and leave any deployment or future-cohort verification pending explicitly. Execute one phase per conversation.

