# SA-031 — Final architecture and operator-documentation consistency check

Priority: **P2** · Sprint: **6** · Provisional estimate: **2 points**

Status lives in [STATE.json](../STATE.json), not this card. Dependencies: [SA-024](SA-024.md), [SA-027](SA-027.md), [SA-029](SA-029.md). Evidence: F23 in the [September audit](../../../audit/2026-09-10-repository-production-review.md).

## Intended result

DOC-001 refreshes the current KT and architecture ahead of remediation at the
user's request. Each story maintains affected documentation as it lands.
This card remains the final consistency check after SA-024, SA-027 and SA-029
are accepted; it is not the first documentation update and is not completed
by the September 15 refresh.

Update living docs to reviewed implementation and verified rollout state, including state ownership, schedule, flags, learning contract and runbooks. Keep dated evidence/specs as history with clear status links.

## Starting points

- `docs/ARCHITECTURE.md`
- `docs/PRODUCT_MAP.md`
- `docs/RL_DESIGN.md`
- `docs/TECHNICAL_DESIGN.md`
- `CODEBASE.md`

Paths are starting points; inspect current callers before editing. Keep this task bounded. If the implementation cannot fit a focused review, record a split with acceptance criteria in STATE.json before taking on another scope.

## Acceptance criteria

- Each runtime/flag claim identifies code default versus observed deployed value.
- Implemented, deployed, verified and statistically demonstrated states are separate.
- The August PI crosswalk and current continuation queue agree with accepted work.
- The per-story documentation changes, source Markdown, generated PDF and
  human testing guide agree with the final accepted scope. Resolve residual
  historical deep-dive conflicts; do not repeat an unnecessary wholesale rewrite.

## Unit and integration evidence

- Check referenced paths, configuration keys and schedule against source.
- Exercise documented local test/restore commands safely; no new behavior tests for prose-only changes.

Use deterministic fixtures and blocked external transports. Record exact commands, environment, results and any unsupported platform in the evidence receipt. Relevant existing tests plus these independent invariants are required; do not merely assert current implementation outputs.

## Hard review

Read as an operator unfamiliar with the chat. Reject stale claims about nonexistent C# directories, never-used fallbacks or proven learning.

Apply [REVIEW.md](../REVIEW.md) in a fresh conversation after implementation. No unresolved critical/high defect may be signed off. If the reviewer changes behavior, return to implementation and retest; a self-review is not the fresh-session review.

## Rollout and rollback

No deployment needed for documentation. Record remaining production verification gaps explicitly.

Local implementation completion and production verification are separate. Read-only inspection is authorized; this card does not authorize sending notifications, deploying, changing production variables, enabling policies, or applying destructive migrations.

## Handoff

At implementation completion, write `evidence/SA-031-implementation.md`, capture the changed-file manifest and diff digest, set status to `review_required`, and update HANDOFF.md. At accepted review write `evidence/SA-031-review.md`, mark code completion, and leave any deployment or future-cohort verification pending explicitly. Execute one phase per conversation.
