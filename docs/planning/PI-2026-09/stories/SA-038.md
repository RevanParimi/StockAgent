# SA-038 — Judge and retire lapsed production-verification milestones

Priority: **P1** · Sprint: **1** · Provisional estimate: **2 points**

Status lives in [STATE.json](../STATE.json), not this card. Dependencies: none. Evidence: §1.1 in the [2026-09-23 production assurance review](../../../audit/2026-09-23-production-assurance-review.md). Added 2026-09-23 at the user's request.

## Intended result

8 of 9 milestones in `config/milestones.yaml` are past their deadline. Each lapsed entry re-raises a `critical` alert every 7 days, which trains the operator to ignore critical alerts. The Atlas cutover is actually done. B1, B2, E1, A1, F3 and hard-bind each have a specific remaining check, listed in the audit's §1.1.

## Starting points

- `config/milestones.yaml`
- `core/ops/watchdog/checks.py` (`manual_confirmation`)
- User-run read-only probes (`analysis_data/`, ignored)

Paths are starting points; inspect current callers before editing. Keep this task bounded. If the implementation cannot fit a focused review, record a split with acceptance criteria in STATE.json before taking on another scope.

## Acceptance criteria

- Each lapsed entry is judged with a sanitized evidence note, then removed, or re-dated with a stated reason.
- B6 (the data-health section half) is carried to SA-002; the hard-bind review is folded into SA-012/SA-014.
- `ipo_p0_live_window_check` stays open until IPO capture is verified in production.
- Afterwards, no lapsed entry exists without a dated reason.

## Unit and integration evidence

- Probe summaries (counts only) for B1, A1(b) and F3.
- The next watchdog run shows no `critical` for the closed ids.

Use deterministic fixtures and blocked external transports. Record exact commands, environment, results and any unsupported platform in the evidence receipt. Relevant existing tests plus these independent invariants are required; do not merely assert current implementation outputs.

## Hard review

Do not close an entry just to silence it: each removal needs evidence or an explicit decision recorded as such.

Apply [REVIEW.md](../REVIEW.md) in a fresh conversation after implementation. No unresolved critical/high defect may be signed off. If the reviewer changes behavior, return to implementation and retest; a self-review is not the fresh-session review.

## Rollout and rollback

A registry edit reaches production only on deploy; `registry_is_current` watches that.

Local implementation completion and production verification are separate. Read-only inspection is authorized; this card does not authorize sending notifications, deploying, changing production variables, enabling policies, or applying destructive migrations.

## Handoff

At implementation completion, write `evidence/SA-038-implementation.md`, capture the changed-file manifest and diff digest, set status to `review_required`, and update HANDOFF.md. At accepted review write `evidence/SA-038-review.md`, mark code completion, and leave any deployment or future-cohort verification pending explicitly. Execute one phase per conversation.
