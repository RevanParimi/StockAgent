# Core
openai>=1.30.0            # OpenAI-compatible SDK (used with OpenRouter)
pydantic>=2.5.0           # Data models and validation
python-dotenv>=1.0.0      # .env file loading

# Data fetching (future integrations)
httpx>=0.27.0             # Async HTTP client
beautifulsoup4>=4.12.0    # HTML parsing for web scraping
pandas>=2.2.0             # Tabular data handling
yfinance>=0.2.40          # Yahoo Finance OHLCV data (pattern analysis)

# Phase 4 – scheduler
apscheduler>=3.10.0       # Cron-based periodic trigger

# Phase 2 – technical indicators
numpy>=1.26.0             # RSI / MACD / BB computation
requests>=2.31.0          # Serper + NewsAPI HTTP calls

# Phase 3 – RAG pipeline
chromadb>=0.5.0           # Local vector store (ChromaDB)
sentence-transformers>=3.0.0  # Local embeddings (no API key needed)
pypdf>=4.0.0              # PDF document ingestion
tiktoken>=0.7.0           # Token counting for chunking

# Optional cloud vector stores (uncomment if needed)
# pinecone-client>=4.0.0
# qdrant-client>=1.9.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-cov>=5.0.0
