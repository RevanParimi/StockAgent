# Core
langgraph>=0.2.0          # Multi-sector LangGraph flows (graphs/ directory)
openai>=1.30.0            # OpenAI-compatible SDK — used for OpenRouter (Qwen via OpenRouter)
pydantic>=2.5.0           # Data models and validation
python-dotenv>=1.0.0      # .env file loading

# Phase 2 – FastAPI bridge (Python API server consumed by TypeScript + C#)
fastapi>=0.111.0          # REST API + WebSocket server
uvicorn[standard]>=0.30.0 # ASGI server for FastAPI
httpx>=0.27.0             # Async HTTP client + TestClient for FastAPI tests

# Data fetching
beautifulsoup4>=4.12.0    # HTML parsing for web scraping
pandas>=2.2.0             # Tabular data handling
yfinance>=0.2.40          # Yahoo Finance OHLCV data (pattern analysis)
requests>=2.31.0          # Serper + NewsAPI HTTP calls

# Phase 4 – Python scheduler (kept as fallback; C# Quartz.NET is primary)
apscheduler>=3.10.0       # Cron-based periodic trigger

# Technical indicators (numpy; C++ extension is an optional drop-in)
numpy>=1.26.0             # RSI / MACD / BB computation

# RAG pipeline (optional — disabled by default via RAG_ENABLED=false)
chromadb>=0.5.0           # Local vector store (ChromaDB)
sentence-transformers>=3.0.0  # Local embeddings (no API key needed)
pypdf>=4.0.0              # PDF document ingestion
tiktoken>=0.7.0           # Token counting for chunking

# Optional cloud vector stores (uncomment if needed)
# pinecone-client>=4.0.0
# qdrant-client>=1.9.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-cov>=5.0.0
